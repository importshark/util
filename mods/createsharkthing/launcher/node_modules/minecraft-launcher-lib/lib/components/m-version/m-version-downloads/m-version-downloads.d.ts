import { Artifact, IArtifact } from '../../artifact';
export interface IMVersionDownloads {
    client: Partial<IArtifact>;
    server: Partial<IArtifact>;
}
export declare class MVersionDownloads implements IMVersionDownloads {
    static from(child: Partial<IMVersionDownloads>, parent?: Partial<IMVersionDownloads>): MVersionDownloads;
    private _client;
    private _server;
    constructor(client: Partial<IArtifact>, server: Partial<IArtifact>);
    get client(): Artifact;
    set client(client: Artifact);
    get server(): Artifact;
    set server(server: Artifact);
    toJSON(): IMVersionDownloads;
}
