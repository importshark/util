"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.MVersionAssetIndexArtifact = void 0;

var _artifact = require("../../artifact");

class MVersionAssetIndexArtifact extends _artifact.Artifact {
  static from(child, parent) {
    if (!parent) {
      if (child instanceof MVersionAssetIndexArtifact) return child;
      parent = {};
    }

    const {
      id = parent.id,
      totalSize = parent.totalSize
    } = child;
    if (!id) throw new Error('missing minecraft version asset index id');
    if (!totalSize) throw new Error('missing minecraft version asset index total size');

    const {
      path,
      url,
      size,
      sha1
    } = _artifact.Artifact.from(child, parent);

    return new MVersionAssetIndexArtifact(id, totalSize, path, url, size, sha1);
  }

  constructor(id, totalSize, path, url, size, sha1) {
    super(path, url, size, sha1);
    this.id = id;
    this.totalSize = totalSize;
  }

}

exports.MVersionAssetIndexArtifact = MVersionAssetIndexArtifact;
//# sourceMappingURL=m-version-asset-index-artifact.js.map