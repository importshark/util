import { IArtifact } from '../../artifact';
import { ArtifactTask } from '../artifact-task';
export declare const enum ArtifactDownloadTaskEvent {
    PROGRESS = "progress",
    ERROR = "error",
    FINISH = "finish"
}
export interface IArtifactDownloadTaskProgress {
    recieved: number;
    total: number;
}
export declare class ArtifactDownloadTask extends ArtifactTask {
    readonly root: string;
    constructor(artifact: Partial<IArtifact>, root: string);
    start(): Promise<this>;
}
