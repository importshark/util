import { Authenticator, IAuth, IAuthProvider } from '../authenticator';
export declare class OfflineAuthenticator extends Authenticator implements IAuthProvider {
    username: string;
    uuid: string;
    constructor(username: string, uuid?: string);
    getAuth(): IAuth;
}
