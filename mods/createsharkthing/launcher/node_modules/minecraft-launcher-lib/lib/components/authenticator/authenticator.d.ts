export interface IAuth {
    accessToken: string;
    user: {
        name: string;
        type: string;
        uuid: string;
        properties: {
            [prop: number]: {
                name: string;
                value: string;
            };
        };
    };
}
export interface IAuthProvider {
    getAuth(): IAuth;
}
export declare abstract class Authenticator {
    /**
     * Generate a random token (by UUID v4).
     * @returns a random token.
     */
    static generateToken: (replaceSep?: boolean, sep?: string) => string;
}
