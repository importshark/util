export interface IFolder {
    root: string;
}
export declare class Folder implements IFolder {
    root: string;
    static from(location: Location): Folder;
    constructor(root: string);
    join(...parts: string[]): string;
    toString(): string;
    toJSON(): string;
}
export declare type Location = string | Partial<IFolder>;
